# ./.tmuxinator.yml

name: mono
root: ~/smrtzr/smrtzr/

windows:
  - vim: nvim . 
  - services:
      layout: main-vertical
      panes:
        - yarn core
        - cd ~/smrtzr/editor; ./gradlew run
        - cd emulators/docker; docker-compose up
